# 工具规范：gogogo.sh

## 定位

`gogogo.sh` 是当前仓库的统一脚本入口，用于开发、构建、测试、lint、Docker 启动与清理。

## 当前支持命令

- `dev`
- `build`
- `test`
- `lint`
- `clean`

## 使用原则

- `test` 用于统一运行后端测试
- `build` 用于统一触发前端构建与后端编译
- `lint` 用于反映前端当前格式/规范状态
- `clean` 需要保留 `web/dist/index.html` 占位文件，避免影响后端 embed
- Docker 不再作为当前项目正式主工作流
