# 质量规范：页面

## 核心规则

- 本规范适用于所有已有页面的改造、重做、迁移与替换决策。
- 页面开发默认不做一刀切判断，不应直接假设必须重写旧页或必须继续堆在旧页上。
- 每次进入页面改造前，必须先判断现有页面对目标需求的功能覆盖度。

## 实施要求

- 在决定实现方式前，至少应判断主要功能覆盖度、信息结构变化、主交互流程变化、权限边界变化，以及直接修改旧页面的回归风险。
- 如果这些问题尚未明确，不应直接进入实现。
- 本文件只回答页面改造前如何判断，不负责具体功能字段方案。
