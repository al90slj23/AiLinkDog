# 前端规范：构建

## 核心规则

- 前端构建使用 `bun run build`。
- 构建输出位于 `web/dist`。
- 后端 `main.go` 对 `web/dist` 有 embed 依赖，`web/dist/index.html` 还承担后端 embed 占位职责。

## 实施要求

- 前端构建产物不是单纯前端问题，会直接影响后端测试与编译行为。
- 从 AiLinkDog 开始，前端不再把多步初始化向导作为主路径保留。
- 数据库和运行模式不再通过引导式 Setup Wizard 向用户传达“多种正式选择”。
