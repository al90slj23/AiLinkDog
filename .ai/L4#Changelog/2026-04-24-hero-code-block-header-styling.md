## 规范同步判断

- L4 工作日志：必须补写
- 是否涉及长期规则变化：否（此为前端 UI 的轻度视觉调整）

## 代码文件行数检查

- `web/src/pages/Home/home.css`：1788 行左右；**需要关注**；可能影响层：L3（样式规范）、L4（未来重构日志）。行数继续保持较高水平。

## 建议更新层

- L4 工作日志：必须补写。主要记录 `LandingHeroCodeBlock` 中右侧 API 说明文字由于过于像一个普通 Tab 按钮而被要求调整样式的设计意图，以及采用半透明、小字号、等宽字体加引导点进行视觉弱化和区分的重构动作。
- 其它层（L0-L3, L5）：无需同步。

## 需要用户确认的事项

- [ ] 是否同意直接生成 UI 细节微调的 L4 工作日志 `2026-04-24-hero-code-block-header-styling.md`？