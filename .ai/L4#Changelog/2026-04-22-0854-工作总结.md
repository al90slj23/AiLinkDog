# 知识库纯化建议与计划

## 本次工作总结
本次工作日志摘要已生成并记录至 `.ai/L4#Changelog`。基于对改动的分析，识别出 `.ai/` 记忆体系中若干需要同步或澄清的文件。核心改动包括：开发工具流程升级、项目规范迁移至 `.ai/` 体系、新增 Referral 功能基础代码。以下将针对 `.ai/L0` 至 `.ai/L3` 层中受影响的文件提出纯化建议。

## 需要纯化的文件

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L0#Execution/README.md
- **触发原因**：文件内容新增了 `skills/` 目录的说明。
- **旧规范 / 旧认知**：原文件仅预留 `hooks/`、`templates/`、`workflows/` 等目录。
- **新设定 / 新事实**：新增了 `skills/` 目录，并说明其按 `global/` 与 `project/` 分层。
- **冲突点**：未发现明确冲突。新增内容是对执行层目录结构的补充说明，与原有结构兼容。
- **建议纯化位置**：文件中的目录列表部分。
- **建议修改动作**：无需修改。当前改动（新增 `skills/` 说明）已正确更新文件，是对知识库的必要补充。
- **关联改动来源**：`git diff` 显示对该文件的修改。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L2#Index/tech.md
- **触发原因**：文件内容更新了技术栈描述，将 `air` 和 `gogogo.sh dev` 明确为主路径。
- **旧规范 / 旧认知**：原文件仅列出 Go、Bun、Vite、`gogogo.sh` 为工具栈。
- **新设定 / 新事实**：明确了 Vite 用于开发服务器与构建，新增 Air 用于后端本地开发自动重启，并指出 `./gogogo.sh dev` 是本地开发主路径。
- **冲突点**：未发现明确冲突。更新内容是对当前实际开发实践的准确描述，与现有技术栈列表是补充关系。
- **建议纯化位置**：文件中的“开发与构建工具”部分。
- **建议修改动作**：无需修改。当前改动已正确同步了最新的技术实践。
- **关联改动来源**：`git diff` 显示对该文件的修改。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/09.tool-01.gogogo.md
- **触发原因**：文件内容新增了 `dev`、`stop`、`platform` 等命令的行为原则。
- **旧规范 / 旧认知**：原文件仅列出了命令列表和部分原则。
- **新设定 / 新事实**：
    1.  `dev` 是本地开发主入口，使用 `tmux` 三窗格同时管理前后端及监控。
    2.  `dev` 前端使用 `bun run dev` (Vite HMR)，后端使用 `air` (自动编译重启)。
    3.  `stop` 与 `dev` 内部重启都需要清理特定工作区的残留进程（`air`、`vite`、旧后端进程等）。
    4.  `platform` 用于 AI 开发平台切换与 symlink 管理。
- **冲突点**：未发现明确冲突。新增原则是对 `gogogo` 工具最新行为的规范性描述，与原有命令列表构成完整指南。
- **建议纯化位置**：文件中的“使用原则”部分。
- **建议修改动作**：无需修改。当前改动已成功将最新的工具设计决策转化为项目规范。
- **关联改动来源**：`git diff` 显示对该文件的修改；关联设计文档 `docs/superpowers/specs/2026-04-21-gogogo-tmux-three-pane-monitor-design.md` 等。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/02.backend-01.json.md
- **触发原因**：代码改动 (`controller/user.go`) 已遵循 Rule 1，使用 `common` 包函数替代 `encoding/json`。
- **旧规范 / 旧认知**：Rule 1 规定所有 JSON marshal/unmarshal 操作必须使用 `common/json.go` 的包装函数。
- **新设定 / 新事实**：`controller/user.go` 中的实践已符合规范，将多处 `encoding/json` 调用替换为 `common.DecodeJson`、`common.Marshal`、`common.Unmarshal`。
- **冲突点**：未发现冲突。代码实践与现有规范完全一致，是规范的成功落地案例。
- **建议纯化位置**：无需修改规范文件。
- **建议修改动作**：**建议补充同步**。可在该规范文件（如示例部分）或相关总结中，引用 `controller/user.go` 作为遵循 Rule 1 的正面示例，以强化规范。
- **关联改动来源**：`git diff` 中 `controller/user.go` 的修改内容。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/02.backend-05.constants.go
- **触发原因**：`common/constants.go` 新增了多个 `Referral` 相关的业务配置变量。
- **旧规范 / 旧认知**：该规范文件应定义常量文件的编写规则（如分组、命名、初始化等）。
- **新设定 / 新事实**：代码中新增了 `ReferralEnabled`、`ReferralDefaultLinkValidityDays` 等约10个以 `Referral` 为前缀的配置变量。
- **冲突点**：未发现明确冲突。新增变量符合 Go 变量命名规范，并集中在相关功能区域。需要确认规范是否已涵盖“业务功能配置常量”这类别的管理方式。
- **建议纯化位置**：规范中关于常量分类或新增常量注意事项的部分。
- **建议修改动作**：**建议补充同步**。检查并确认规范是否明确允许或提供了添加此类业务功能开关和配置参数的指导。如果规范缺失，可补充说明：业务功能常量应以功能前缀分组，并明确其默认值（如 `false`， `0`）的含义。
- **关联改动来源**：`git diff` 中 `common/constants.go` 的修改内容。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/04.quality-03.git.md
- **触发原因**：`gogogo.memory.sh` 中实现了 Git Diff 截断逻辑（`MEMORY_GIT_DIFF_BYTE_LIMIT=32768`）。
- **旧规范 / 旧认知**：项目 Git 规范可能涉及提交信息、分支策略等，但不一定包含工具脚本中如何处理大量 Diff 的规则。
- **新设定 / 新事实**：记忆同步流程为避免超大 Diff 导致 AI 上下文溢出，主动将 `git diff` 输出截断至前 32768 字节。
- **冲突点**：未发现与现有 Git 提交或工作流规范冲突。这是一个工具层面的优化策略。
- **建议纯化位置**：规范中关于工具与自动化脚本处理 Git 数据的部分，或新增关于“上下文管理”的说明。
- **建议修改动作**：**建议补充同步**。在 Git 或相关质量规范中，添加一条说明：在自动化脚本（尤其是与 AI 交互的脚本）中处理 `git diff` 时，应考虑设置合理的输出大小限制，以防止上下文过长，并应明确提示截断行为。
- **关联改动来源**：`git diff` 中 `gogogo.memory.sh` 的修改内容；关联设计文档 `docs/superpowers/specs/2026-04-22-gogogo-memory-context-truncation-design.md`。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/10.ai-01.memory-02.usage.md
- **触发原因**：`gogogo.memory.sh` 将记忆同步的输出目标从 `L5#Knowledge/recent-fix-summary.md` 改为 `L4#Changelog`，并视其为“工作日志”。
- **旧规范 / 旧认知**：原流程可能将总结输出至 `L5` 或未明确 `L4` 作为工作日志的默认位置。
- **新设定 / 新事实**：
    1.  “近期工作总结”按工作日志理解，默认归入 `L4#Changelog`。
    2.  `L5` 当前不作为此流程的输出目标。
    3.  日志文件命名推荐格式为 `YYYY-MM-DD-HHMM-主题.md`。
- **冲突点**：与旧流程的目标文件路径冲突。新设定重新定义了 `L4` 的用途（工作日志）和 `L5` 在此场景下的非目标角色，这属于流程规则的重大更新。
- **建议纯化位置**：该规范文件应明确描述 `gogogo.sh 7` (记忆同步) 流程的输出目的地、文件命名规范以及各层级的职责。
- **建议修改动作**：**必须修改**。更新规范，以反映新的设定：
    - 明确 `gogogo.sh 7` 生成的“工作日志摘要”应保存至 `L4#Changelog/` 目录下按时间戳命名的文件中。
    - 说明 `L4` 用于记录按时间排序的工作事实与日志。
    - 说明在此流程中 `L5` 的暂不使用的状态。
    - 加入文件命名格式要求。
- **关联改动来源**：`git diff` 中 `gogogo.memory.sh` 的修改内容（特别是 `MEMORY_L4_WORK_LOG_FILE` 变量和 `update_l4_work_log_file` 函数）；`git diff` 中 `.ai/L4#Changelog/README.md` 新增的“文件命名”章节。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/10.ai-01.memory-03.skills.md
- **触发原因**：`.ai/L0#Execution/` 下新增了 `skills/` 目录，并提及按 `global/` 与 `project/` 分层。
- **旧规范 / 旧认知**：该规范文件应定义技能 (Skills) 的创建、管理、使用规则。
- **新设定 / 新事实**：在 `L0#Execution` 层创建了 `skills/` 目录作为技能源文件的存放点，并规划了 `global/`（全局）与 `project/`（项目）的子目录分层结构。
- **冲突点**：未发现明确冲突。这是对技能存储位置的具体落实。需要确认现有规范是否已预见到此目录结构，以及 `global/` 和 `project/` 分层的具体管理规则是否明确。
- **建议纯化位置**：规范中关于技能存储、分类、优先级的部分。
- **建议修改动作**：**建议补充同步**。在技能规范中，明确说明：
    1.  技能源文件存放于 `.ai/L0#Execution/skills/`。
    2.  采用 `global/` 与 `project/` 两级目录进行分层管理。
    3.  定义 `global` 技能与 `project` 技能的区别（例如，通用工具技能 vs 项目特定流程技能）和加载/使用优先级。
- **关联改动来源**：`git diff` 中 `.ai/L0#Execution/README.md` 的修改内容；新增的 `tests/gogogo_opencode_global_skills_test.sh` 等测试文件。

## 纯化执行计划
1.  **立即执行（无需修改）**：
    *   确认 `.ai/L0#Execution/README.md`、`.ai/L2#Index/tech.md`、`.ai/L3#Standards/standards/09.tool-01.gogogo.md` 的当前内容已正确，无需额外操作。

2.  **高优先级（必须修改）**：
    *   **目标**：`.ai/L3#Standards/standards/10.ai-01.memory-02.usage.md`
    *   **动作**：根据“新设定/新事实”更新该文件，明确记忆同步流程的输出目标、`L4` 的日志定位、文件命名规范。

3.  **中优先级（建议补充）**：
    *   **目标**：`.ai/L3#Standards/standards/02.backend-01.json.md`
    *   **动作**：在文件中添加一个注释或示例，引用 `controller/user.go` 的改动作为遵循 Rule 1 的范例。
    *   **目标**：`.ai/L3#Standards/standards/02.backend-05.constants.go`
    *   **动作**：审查并补充关于添加业务功能配置常量的指导原则（命名前缀、默认值语义）。
    *   **目标**：`.ai/L3#Standards/standards/04.quality-03.git.md`
    *   **动作**：添加关于在自动化脚本中限制 `git diff` 输出大小的最佳实践说明。
    *   **目标**：`.ai/L3#Standards/standards/10.ai-01.memory-03.skills.md`
    *   **动作**：补充技能文件存储路径 (`L0#Execution/skills/`) 和 `global`/`project` 分层管理的具体规则。

## 需要用户确认的事项
1.  对于 **`.ai/L3#Standards/standards/10.ai-01.memory-02.usage.md`** 的修改建议（将记忆同步输出定向至 `L4#Changelog` 并定义为工作日志），是否与您对 AI 记忆体系层级的整体规划相符？确认后即可执行修改。
2.  关于 **技能规范** 中 `global` 与 `project` 分层的具体区别和优先级规则，您是否有更详细的定义需要我写入规范？还是由我根据现有上下文（如测试文件命名）推断并起草？
3.  本次纯化计划是否涵盖了所有您关心的冲突点？是否有其他未在“已选择详细阅读的目标文件”列表中，但您认为需要检查的 `.ai/` 文件？
