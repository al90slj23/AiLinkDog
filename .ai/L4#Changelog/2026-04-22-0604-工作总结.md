# 知识库纯化建议与计划

## 本次工作总结
基于对最近工作总结和Git改动的分析，本次工作主要涉及本地开发流程重构、AI协作体系完善以及Referral新功能开发。这些改动对知识库（.ai/目录）的多个层级产生了影响，主要集中在L0执行层、L2索引层、L3标准层和L4变更日志层。需要对这些文件进行纯化，以确保知识库内容与当前项目状态保持一致。

## 需要纯化的文件

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L0#Execution/README.md
- **触发原因**：文件被修改，新增了`skills/`目录的说明。
- **旧规范 / 旧认知**：L0执行层目录结构未明确包含`skills/`目录。
- **新设定 / 新事实**：L0执行层新增了`skills/`目录，用于存放项目级技能源文件，按`global/`与`project/`分层。
- **冲突点**：文件内容已更新，与当前项目结构一致。未发现与知识库其他部分存在冲突。
- **建议纯化位置**：当前文件内容已是最新。
- **建议修改动作**：无需修改。文件内容已正确反映新增的`skills/`目录结构。
- **关联改动来源**：`git diff`显示对`.ai/L0#Execution/README.md`的修改。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L2#Index/tech.md
- **触发原因**：文件被修改，更新了技术栈描述。
- **旧规范 / 旧认知**：技术栈描述可能未完全反映当前基于`air`和`vite`的本地开发实践。
- **新设定 / 新事实**：技术栈明确包含`Air`用于后端本地开发自动编译重启，`Vite`用于前端开发服务器与构建，并将`./gogogo.sh dev`标注为本地开发主路径。
- **冲突点**：文件内容已更新，与当前开发实践一致。未发现与知识库其他部分存在冲突。
- **建议纯化位置**：当前文件内容已是最新。
- **建议修改动作**：无需修改。文件已准确描述当前技术栈。
- **关联改动来源**：`git diff`显示对`.ai/L2#Index/tech.md`的修改。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/09.tool-01.gogogo.md
- **触发原因**：文件被修改，更新了`gogogo`工具的使用原则。
- **旧规范 / 旧认知**：文档可能未涵盖`dev`、`stop`、`platform`等命令的最新语义和`tmux`三窗格开发环境。
- **新设定 / 新事实**：文档已更新，明确了`dev`是本地开发主入口（使用`tmux`拉起前后端及监控）、`dev`前后端分别使用`air`和`vite`、`stop`需要清理进程、`platform`用于AI开发平台管理等原则。
- **冲突点**：文件内容已更新，与`gogogo.sh`及`gogogo.1.sh`的实现一致。未发现与知识库其他部分存在冲突。
- **建议纯化位置**：当前文件内容已是最新。
- **建议修改动作**：无需修改。文档已同步了工具的最新使用原则。
- **关联改动来源**：`git diff`显示对`.ai/L3#Standards/standards/09.tool-01.gogogo.md`的修改。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/02.backend-05.constants.go
- **触发原因**：`common/constants.go`被修改，新增了Referral相关配置变量。
- **旧规范 / 旧认知**：常量文件中可能未定义Referral功能的配置变量。
- **新设定 / 新事实**：`common/constants.go`中新增了`ReferralEnabled`、`ReferralDefaultLinkValidityDays`等9个以`Referral`为前缀的配置变量，默认值均为`false`或`0`，符合项目常量命名规则。
- **冲突点**：未发现明确冲突。新增常量遵循了现有命名规范（驼峰式，首字母大写），且默认值设置合理（功能默认关闭）。
- **建议纯化位置**：`.ai/L3#Standards/standards/02.backend-05.constants.go`（如果该文件存在，用于记录常量规范）。
- **建议修改动作**：**需要创建或更新**。建议在该标准文档中补充说明：新增业务功能（如Referral）时，应在`common/constants.go`中定义相关配置变量，并明确其默认值（通常新功能默认关闭）。变量命名应清晰反映其所属功能域。
- **关联改动来源**：`git diff`显示对`common/constants.go`的修改，新增了Referral相关变量。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/02.backend-01.json.md
- **触发原因**：`controller/user.go`的改动统一使用了`common/json.go`中的方法。
- **旧规范 / 旧认知**：规则1已明确要求所有JSON操作必须使用`common/json.go`中的包装函数。
- **新设定 / 新事实**：`controller/user.go`中的代码修改遵循了此规则，将`encoding/json`的直接调用替换为`common.DecodeJson`、`common.Marshal`、`common.Unmarshal`。
- **冲突点**：未发现冲突。代码改动是规则1的具体实践，符合现有规范。
- **建议纯化位置**：当前规则文档（如果存在）内容应已涵盖此要求。
- **建议修改动作**：**仅建议补充同步**。建议在规则文档中强化或添加示例，说明在控制器（Controller）中处理请求体（`c.Request.Body`）和响应数据时，应统一使用`common`包下的JSON方法，以保持项目一致性。
- **关联改动来源**：`git diff`显示对`controller/user.go`的修改，涉及多处JSON处理代码的替换。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/02.backend-03.database.md
- **触发原因**：数据库迁移函数`migrateDB`新增了Referral相关表。
- **旧规范 / 旧认知**：规则2明确了PostgreSQL是唯一官方数据库方向。数据库迁移文档可能未包含新增表结构的说明。
- **新设定 / 新事实**：`model/main.go`中的`migrateDB`和`migrateDBFast`函数已添加`ReferralPlan`、`ReferralAccount`、`ReferralLink`、`ReferralCommission`、`ReferralWithdrawal`五个新模型。
- **冲突点**：未发现与PostgreSQL-only方向冲突。新增表符合项目数据模型规范。
- **建议纯化位置**：`.ai/L3#Standards/standards/02.backend-03.database.md`（如果该文件存在，用于记录数据库规则）。
- **建议修改动作**：**需要创建或更新**。建议在该标准文档中：
    1.  重申规则2（PostgreSQL-only）。
    2.  补充说明：当新增业务功能涉及持久化数据时，需在`model/`目录下创建对应的GORM模型文件，并确保在`model/main.go`的`migrateDB`系列函数中注册，以自动生成或更新表结构。
    3.  可考虑添加一个简单的模型创建与迁移流程示例。
- **关联改动来源**：`git diff`显示对`model/main.go`的修改，新增了Referral相关模型的注册。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/10.ai-01.memory-02.usage.md
- **触发原因**：`gogogo.memory.sh`的改动将记忆同步流程的输出目标从`L5#Knowledge`调整为`L4#Changelog`。
- **旧规范 / 旧认知**：记忆同步流程可能默认或曾经将总结输出到`L5#Knowledge`。
- **新设定 / 新事实**：`gogogo.memory.sh`脚本中的`MEMORY_RECENT_FIX_SUMMARY`路径变量已被替换，逻辑更新为将工作日志总结写入`L4#Changelog`目录下的特定文件（`2026-04-20-1045-...md`），并明确说明`L5`当前不作为输出目标。
- **冲突点**：未发现明确冲突。此变更是对AI协作体系工作流的一次明确收敛，将“近期工作总结”归类为工作日志，存入`L4`。
- **建议纯化位置**：`.ai/L3#Standards/standards/10.ai-01.memory-02.usage.md`（如果该文件存在，用于记录记忆体系使用规则）。
- **建议修改动作**：**需要创建或更新**。建议在该标准文档中明确规定：
    1.  `gogogo.sh 7`（记忆同步）流程的输出目标是`L4#Changelog`。
    2.  工作日志（记录每次协作的事实、决策与上下文）应存放于`L4#Changelog`，并遵循`YYYY-MM-DD-HHMM-主题.md`的命名格式。
    3.  `L5#Knowledge`的定位（如用于存放提炼后的稳定知识、架构决策等）应与此区分并另行说明。
- **关联改动来源**：`git diff`显示对`gogogo.memory.sh`的修改，包括变量名更改和逻辑调整。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/04.quality-03.git.md
- **触发原因**：`L4#Changelog/README.md`规范了工作日志的文件命名格式。
- **旧规范 / 旧认知**：Git或提交规则可能未包含对`L4`日志文件命名的具体指导。
- **新设定 / 新事实**：`L4#Changelog/README.md`明确要求日志文件使用`YYYY-MM-DD-HHMM-主题.md`格式，主题应直接表达事实，避免使用`recent-*`等滚动语义命名。
- **冲突点**：未发现与现有Git提交规范冲突。这是对项目内部文档（非源码）管理规范的补充。
- **建议纯化位置**：`.ai/L3#Standards/standards/04.quality-03.git.md`（如果该文件存在，用于记录Git与提交规范）。
- **建议修改动作**：**需要创建或更新**。建议在该标准文档中补充一条关于AI协作体系文件命名的规则：`L4#Changelog`下的工作日志文件必须采用`YYYY-MM-DD-HHMM-主题.md`格式，以确保可追溯性和避免歧义。
- **关联改动来源**：`git diff`显示对`.ai/L4#Changelog/README.md`的修改，新增了“文件命名”章节。

## 纯化执行计划
1.  **立即完成（无需操作）**：
    *   确认 `.ai/L0#Execution/README.md`、`.ai/L2#Index/tech.md`、`.ai/L3#Standards/standards/09.tool-01.gogogo.md` 已是最新状态，无需修改。
2.  **需要补充或创建标准文档（高优先级）**：
    *   **创建/更新 `02.backend-05.constants.go.md`**：补充新增业务功能配置变量的定义规范。
    *   **更新 `02.backend-01.json.md`**：强化在控制器中使用`common/json.go`的实践示例。
    *   **创建/更新 `02.backend-03.database.md`**：明确PostgreSQL-only方向，并补充模型创建与迁移流程。
    *   **创建/更新 `10.ai-01.memory-02.usage.md`**：明确规定`gogogo.sh 7`的输出目标为`L4#Changelog`以及日志命名规范。
    *   **更新 `04.quality-03.git.md`**：补充`L4#Changelog`工作日志的文件命名规则。
3.  **验证与同步**：
    *   检查 `.ai/L1#Overview/guide.md` 的内容，确保其作为 `AGENTS.md` 符号链接的目标，涵盖了项目最新的概览、规则和协作约定。
    *   确认所有新增的 `L4#Changelog` 日志文件均符合新的命名规范。

## 需要用户确认的事项
1.  上述计划中提到的需要“创建或更新”的L3标准文档（如`02.backend-05.constants.go.md`）是否已存在？如果存在，请提供其当前内容以便进行精准更新；如果不存在，确认是否同意按建议创建这些文档。
2.  对于`10.ai-01.memory-02.usage.md`中关于`L5#Knowledge`定位的建议（与`L4#Changelog`区分），是否需要进一步明确或调整？
3.  是否同意将`L4#Changelog`的文件命名规范（`YYYY-MM-DD-HHMM-主题.md`）正式纳入项目的Git/文档管理标准？
