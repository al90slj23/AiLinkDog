# 知识库纯化建议与计划

## 本次工作总结
本次工作主要涉及本地开发流程重构（引入 `air`、`tmux` 三窗格）、AI 工具链增强（提交摘要与记忆同步的上下文截断）以及推广中心功能开发。这些改动对 `.ai` 知识库中的技术栈描述、工具使用规范、后端编码规则、Git 操作规范、AI 技能管理及测试规范等文件产生了直接影响，需要进行同步更新。

## 需要纯化的文件

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L2#Index/tech.md
- **触发原因**：`git diff` 显示该文件被修改，更新了本地开发工具栈的描述。
- **旧规范 / 旧认知**：技术栈描述为“Go, Bun, Vite, `gogogo.sh`”。
- **新设定 / 新事实**：本地开发流程已重构，后端引入 `air` 实现自动重载，`gogogo.sh dev` 成为本地开发主路径。
- **冲突点**：知识库中关于本地开发技术栈的描述不完整，缺少 `air` 和 `gogogo.sh dev` 路径的明确说明。
- **建议纯化位置**：`tech.md` 中“开发与构建工具”部分。
- **建议修改动作**：将“开发与构建工具”列表更新为：Go（后端编译与测试）、Bun（前端依赖、构建、lint）、Vite（前端开发服务器与前端构建）、Air（后端本地开发自动编译重启）、`gogogo.sh`（统一入口脚本，本地开发主路径为 `./gogogo.sh dev`）。
- **关联改动来源**：`git diff` 中 `.ai/L2#Index/tech.md` 的修改内容。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/09.tool-01.gogogo.md
- **触发原因**：`git diff` 显示该文件被修改，更新了 `gogogo.sh` 命令的使用原则。
- **旧规范 / 旧认知**：文档可能未详细说明 `dev`、`stop`、`platform` 等命令的具体行为。
- **新设定 / 新事实**：`dev` 命令通过 `tmux` 拉起三窗格（后端 `air`、前端 `vite`、监控脚本），`stop` 和内部重启会清理相关进程，`platform` 用于 AI 平台管理。
- **冲突点**：知识库中关于 `gogogo.sh` 各子命令（尤其是 `dev`）的详细行为描述缺失或过时。
- **建议纯化位置**：`09.tool-01.gogogo.md` 中“使用原则”部分。
- **建议修改动作**：在“使用原则”部分补充以下内容：
    - `dev` 是当前仓库的本地开发主入口，默认通过 `tmux` 同时拉起前后端开发环境。
    - `dev` 的前端 pane 使用 `bun install && bun run dev`，由 `vite` 提供 HMR 或自动刷新。
    - `dev` 的后端 pane 使用 `air`，语义是“检测 `.go` 文件变化后自动编译重启”，不是真正的运行时热替换。
    - `stop` 与 `dev` 内部重启路径都需要按当前 workspace 清理相关本地开发进程，避免残留 `air`、`vite`、旧后端进程或旧 tmux 会话干扰新会话。
    - `platform` 用于统一进入 AI 开发平台切换与 symlink 管理能力。
- **关联改动来源**：`git diff` 中 `.ai/L3#Standards/standards/09.tool-01.gogogo.md` 的修改内容。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/02.backend-01.json.md
- **触发原因**：`controller/user.go` 中大量 `encoding/json` 调用被替换为 `common/json.go` 的包装函数。
- **旧规范 / 旧认知**：规则要求所有 JSON 操作必须使用 `common/json.go` 中的包装函数。
- **新设定 / 新事实**：代码改动（如 `user.go` 中的 `common.DecodeJson`, `common.Marshal`, `common.Unmarshal`）严格遵循了此规则。
- **冲突点**：未发现明确冲突。代码实践符合现有规范。
- **建议纯化位置**：无需修改规范文件。
- **建议修改动作**：无需修改。仅建议在相关工作总结或案例中补充此次 `user.go` 的改动作为遵循 JSON 规则的正面示例。
- **关联改动来源**：`git diff` 中 `controller/user.go` 的修改内容。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/02.backend-05.constants.md
- **触发原因**：`common/constants.go` 中新增了多个以 `Referral` 开头的系统配置变量。
- **旧规范 / 旧认知**：常量文件用于定义系统级的配置变量。
- **新设定 / 新事实**：为支持推广中心功能，新增了 `ReferralEnabled`, `ReferralDefaultLinkValidityDays` 等约9个相关常量。
- **冲突点**：未发现明确冲突。新增常量符合常量文件的用途。
- **建议纯化位置**：无需修改规范文件。
- **建议修改动作**：无需修改。可考虑在常量管理相关的规范或文档中，补充关于新增功能模块时如何定义系统常量的指引。
- **关联改动来源**：`git diff` 中 `common/constants.go` 的修改内容。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/04.quality-03.git.md
- **触发原因**：`gogogo.git.sh` 和 `gogogo.memory.sh` 中引入了对 `git diff` 输出的截断处理（`COMMIT_SUMMARY_DIFF_BYTE_LIMIT`, `MEMORY_GIT_DIFF_BYTE_LIMIT`）。
- **旧规范 / 旧认知**：Git 操作规范可能未涉及处理大型变更时对输出长度的限制。
- **新设定 / 新事实**：AI 摘要生成和记忆同步流程中，为避免提示词过长，对 `git diff` 输出进行了安全截断（默认 32768 字节），并添加了截断提示。
- **冲突点**：现有 Git 操作规范可能缺少对“大变更场景下工具链的稳定性处理”的说明。
- **建议纯化位置**：`04.quality-03.git.md` 中关于提交摘要和 AI 辅助工作流的部分。
- **建议修改动作**：补充说明：在通过 AI 生成提交摘要或同步记忆时，工具链会自动对过长的 `git diff` 输出进行截断（目前限制为 32768 字节），以确保与 AI 模型的交互稳定性。开发者应意识到摘要可能基于部分变更生成。
- **关联改动来源**：`git diff` 中 `gogogo.git.sh` 和 `gogogo.memory.sh` 的修改内容。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L0#Execution/README.md
- **触发原因**：该文件被修改，新增了 `skills/` 目录的规划。
- **旧规范 / 旧认知**：`L0#Execution` 层规划包含 `specs/`, `plans/`, `hooks/`, `templates/`, `workflows/` 目录。
- **新设定 / 新事实**：明确将 `skills/` 目录纳入 `L0#Execution` 层，用于存放项目内 skill 源文件，并按 `global/` 与 `project/` 分层。
- **冲突点**：知识库中 `L0#Execution` 层的目录结构描述不完整。
- **建议纯化位置**：`L0#Execution/README.md` 中“本层负责”和“目录结构”部分。
- **建议修改动作**：
    1.  在“本层负责”的存放列表中，将“预留 `hooks/`、`templates/`、`workflows/` 等执行相关目录”更新为“存放 `skills/`、`hooks/`、`templates/`、`workflows/` 等执行相关目录”。
    2.  在“目录结构”中，增加 `skills/` 条目：“`skills/`：项目内 skill 源文件目录，按 `global/` 与 `project/` 分层”。
- **关联改动来源**：`git diff` 中 `.ai/L0#Execution/README.md` 的修改内容。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/10.ai-01.memory-03.skills.md
- **触发原因**：`L0#Execution` 层新增 `skills/` 目录，且 AI 工具链（`opencode`）增强涉及 skill 的使用。
- **旧规范 / 旧认知**：可能尚未存在关于 AI `skills` 目录管理和使用的明确规范。
- **新设定 / 新事实**：项目规划了 `skills/` 目录结构，并且 `gogogo.ai.sh` 中 `opencode` 的调用已支持通过文件传递大提示词（可视为 skill 的一种使用方式）。
- **冲突点**：知识库中缺乏对 AI `skills` 如何定义、存放和使用的指导。
- **建议纯化位置**：`10.ai-01.memory-03.skills.md`（若不存在则需创建）。
- **建议修改动作**：创建或更新该文件，明确以下内容：
    - **定义**：Skill 是可被 AI 辅助工具（如 `opencode`）调用的、封装了特定指令或上下文的知识单元。
    - **存放位置**：位于 `.ai/L0#Execution/skills/`，分为 `global/`（通用技能）和 `project/`（项目特定技能）。
    - **使用方式**：工具链（如 `gogogo.sh`）在需要时可加载并注入 skill 内容作为 AI 提示词的一部分。
    - **与 `opencode` 的关系**：`opencode run < skill_file` 是使用 skill 的一种模式。
- **关联改动来源**：`git diff` 中 `.ai/L0#Execution/README.md` 的修改、`gogogo.ai.sh` 的修改（`opencode` 调用）以及新增的 `tests/gogogo_opencode_skills_test.sh` 等测试文件。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/04.quality-02.testing.md
- **触发原因**：本次工作新增了大量测试脚本（`tests/` 目录下），覆盖 `air` 配置、`gogogo` 各子命令功能、上下文截断等。
- **旧规范 / 旧认知**：测试规范可能未涵盖对 `gogogo.sh` 这类复合脚本工具的测试方法。
- **新设定 / 新事实**：建立了针对 `gogogo.sh` 及其相关工具链（`dev`, `memory`, `git` 等子命令）的完整测试套件，测试方式包括命令执行、输出验证、文件状态检查等。
- **冲突点**：现有测试规范可能需要扩展，以包含对 shell 脚本/开发工具链进行集成测试的最佳实践描述。
- **建议纯化位置**：`04.quality-02.testing.md` 中关于测试范围和类型的部分。
- **建议修改动作**：补充说明：项目测试不仅包括 Go 后端单元测试和前端测试，也包括对核心开发工具链（如 `gogogo.sh`）的集成测试。这些测试位于 `tests/` 目录，用于验证脚本功能、参数处理、环境清理及与外部工具（`tmux`, `air` 等）的交互是否符合预期。
- **关联改动来源**：`git status` 中新增的 `tests/gogogo_*_test.sh` 系列文件。

## 纯化执行计划
1.  **立即执行（基于明确 diff）**：
    a. 更新 `.ai/L2#Index/tech.md`，同步技术栈描述。
    b. 更新 `.ai/L3#Standards/standards/09.tool-01.gogogo.md`，补充 `dev`, `stop`, `platform` 命令的详细原则。
    c. 更新 `.ai/L0#Execution/README.md`，明确 `skills/` 目录结构。
2.  **补充规范（基于代码实践）**：
    a. 更新 `.ai/L3#Standards/standards/04.quality-03.git.md`，增加大变更截断说明。
    b. 创建/更新 `.ai/L3#Standards/standards/10.ai-01.memory-03.skills.md`，定义 skill 管理规范。
    c. 更新 `.ai/L3#Standards/standards/04.quality-02.testing.md`，补充工具链集成测试说明。
3.  **记录与同步**：
    a. 将本次纯化行动本身记录到 `L4#Changelog`。
    b. 对于未修改规范文件但实践符合规范的案例（如 JSON 规则、常量新增），可在相关总结或案例库中提及，作为范例。

## 需要用户确认的事项
1.  对于计划创建的新规范文件 `10.ai-01.memory-03.skills.md`，其初步内容框架（定义、位置、使用）是否可接受？是否需要补充其他方面（如命名约定、版本管理）？
2.  在 `04.quality-03.git.md` 中关于输出截断的补充说明，其表述（“以确保与 AI 模型的交互稳定性”）是否准确反映了设计意图？
3.  本次分析未发现 `AGENTS.md` 替换为符号链接这一改动与现有知识库内容的直接冲突。此变更仅为文档入口统一，是否确认无需在 `.ai` 知识库内进行额外说明？
