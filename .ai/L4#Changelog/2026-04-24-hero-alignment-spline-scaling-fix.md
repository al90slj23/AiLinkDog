## 规范同步判断

- L4 工作日志：必须补写
- 是否涉及长期规则变化：否（修复 Spline 画布在 Flex/Grid 下意外拉伸导致的自适应巨量放大问题）

## 代码文件行数检查

- `web/src/pages/Home/home.css`：1763 行；**需要关注**；可能影响层：L3（样式规范）、L4（未来重构日志）。
- 行数依然在临界点以上，进一步证明了对复杂落地页进行 CSS 模块化拆分的必要性。

## 建议更新层

- L4 工作日志：必须补写，记录在重构 Grid 布局导致第三方 3D 渲染组件缩放失效时，如何通过剥离外层尺寸（`width: 100%`），恢复并保留原始 `flex-basis: 68%` 和 `transform: scale(0.95)` 限制来成功控制尺寸而不影响外层 DOM 对齐的经验。
- 其它层（L0-L3, L5）：无需同步。

## 需要用户确认的事项

- [ ] 是否同意生成后续 L4 排障总结 `2026-04-24-hero-alignment-spline-scaling-fix.md`？