# 知识库纯化建议与计划

## 本次工作总结
基于对近期工作（本地开发流程增强、项目文档重构、推荐中心功能开发）的总结，以及对相关 `.ai` 知识库文件的详细阅读，本次纯化旨在确保知识库内容与代码实现、项目结构的最新状态保持一致。主要涉及对执行层结构、技术栈说明、工具规范、后端编码规则、数据库规则、常量管理、测试规范及AI技能规则的同步与澄清。

## 需要纯化的文件

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L0#Execution/README.md
- **触发原因**：文件已修改，新增了 `skills/` 目录的说明。
- **旧规范 / 旧认知**：`L0#Execution` 层预留了 `hooks/`、`templates/`、`workflows/` 等目录，未明确包含 `skills/`。
- **新设定 / 新事实**：`L0#Execution` 层明确增加了 `skills/` 目录，用于存放项目级和全局技能源文件，并按 `global/` 与 `project/` 分层。
- **冲突点**：无冲突。文件修改已正确反映了新的目录结构。
- **建议纯化位置**：文件当前内容。
- **建议修改动作**：无需修改。文件已更新为最新状态。
- **关联改动来源**：`git diff` 显示对 `.ai/L0#Execution/README.md` 的修改。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L2#Index/tech.md
- **触发原因**：文件已修改，更新了技术栈中关于 `air` 和 `gogogo.sh dev` 的说明。
- **旧规范 / 旧认知**：技术栈列表包含 Go、Bun、Vite、`gogogo.sh`。
- **新设定 / 新事实**：技术栈列表更新为 Go、Bun、Vite、Air、`gogogo.sh`。并明确 `air` 用于后端本地开发自动编译重启，`gogogo.sh dev` 是本地开发主路径。
- **冲突点**：无冲突。更新准确反映了当前开发工具链。
- **建议纯化位置**：文件当前内容。
- **建议修改动作**：无需修改。文件已更新为最新状态。
- **关联改动来源**：`git diff` 显示对 `.ai/L2#Index/tech.md` 的修改。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/09.tool-01.gogogo.md
- **触发原因**：文件已修改，详细更新了 `gogogo` 工具的使用原则。
- **旧规范 / 旧认知**：文档列出了 `gogogo` 的主要命令（dev, build, test, lint, clean, deploy）和简要的使用原则。
- **新设定 / 新事实**：命令列表增加了 `platform`、`attach`、`stop`。使用原则部分详细说明了 `dev` 模式的行为（tmux三窗格、前后端分工、`air` 的作用）、`stop` 的清理逻辑以及 `platform` 命令的用途。
- **冲突点**：无冲突。更新内容是对原有规范的细化和补充，与代码实现（`gogogo.sh`, `gogogo.1.sh`, `gogogo.lib.sh`）一致。
- **建议纯化位置**：文件当前内容。
- **建议修改动作**：无需修改。文件已更新为最新、最详细的规范。
- **关联改动来源**：`git diff` 显示对 `.ai/L3#Standards/standards/09.tool-01.gogogo.md` 的修改。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/02.backend-01.json.md
- **触发原因**：代码改动（`controller/user.go`）中大量使用 `common.DecodeJson`、`common.Marshal`、`common.Unmarshal` 替换了直接的 `encoding/json` 调用，遵循了 JSON 包装器规则。
- **旧规范 / 旧认知**：规则要求所有 JSON marshal/unmarshal 操作必须使用 `common/json.go` 中的包装器函数。
- **新设定 / 新事实**：代码实践严格遵循了此规则。未发现规范文件本身需要更新。
- **冲突点**：未发现明确冲突。代码改动是规则的良好实践。
- **建议纯化位置**：无。
- **建议修改动作**：无需修改。规范文件内容正确，且代码实践已与之对齐。
- **关联改动来源**：`git diff` 中 `controller/user.go` 的修改内容。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/02.backend-03.database.md
- **触发原因**：新增了多个 `referral_*.go` 数据模型文件，并在 `model/main.go` 的迁移函数中注册。
- **旧规范 / 旧认知**：数据库规则（如果存在）应涵盖模型定义、迁移和 PostgreSQL 方向。
- **新设定 / 新事实**：新增了推荐系统相关的数据模型（`ReferralPlan`, `ReferralAccount` 等），并已集成到标准迁移流程中。这属于在现有 PostgreSQL 方向上的功能扩展。
- **冲突点**：未发现明确冲突。新增模型符合“PostgreSQL Only”的数据库方向，并正确使用了 GORM 的 `AutoMigrate`。
- **建议纯化位置**：无。
- **建议修改动作**：无需修改。新增模型是业务功能的正常扩展，未违反现有数据库规则。可考虑未来在规范中补充关于新增模型命名和迁移集成的示例，但非本次必需。
- **关联改动来源**：新增的 `model/referral_*.go` 文件及 `model/main.go` 的 `diff` 片段。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/02.backend-05.constants.go
- **触发原因**：`common/constants.go` 中新增了多个推荐系统配置变量（如 `ReferralEnabled`）。
- **旧规范 / 旧认知**：常量规则（如果存在）应指导常量的定义、命名和用途。
- **新设定 / 新事实**：新增了以 `Referral` 为前缀的配置变量，并赋予了默认值（如 `false`, `0`）。同时，`SystemName` 常量从 “New API” 更改为 “AiLink.Dog”。
- **冲突点**：
    1.  **`SystemName` 变更**：此变更将系统显示名称从上游标识改为本地维护标识。需要评估这是否违反“Protected Project Information”规则（规则5）。规则5保护的是 **nеw-аρi** 和 **QuаntumΝоuѕ** 的项目与组织身份标识，通常指代代码所有权、版权、品牌溯源等。`SystemName` 作为面向用户的可配置显示名称，其变更可能被视为前端品牌化的一部分，而非篡改核心项目身份。**需要用户确认**。
    2.  新增的 `Referral*` 变量本身无冲突，是功能扩展的正常操作。
- **建议纯化位置**：规则5（如果存在于其他文件，如原 `AGENTS.md` 的精神）的解读澄清；或在本常量规范中补充关于可配置显示名称与受保护标识的区分说明。
- **建议修改动作**：**暂停执行**。等待用户对 `SystemName` 变更的合规性进行确认。
- **关联改动来源**：`git diff` 中 `common/constants.go` 的修改内容。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/04.quality-02.testing.md
- **触发原因**：新增了大量测试脚本（`tests/gogogo_*_test.sh`）和 Go 测试文件（`*_test.go`）。
- **旧规范 / 旧认知**：测试规范应涵盖测试的编写、组织和执行。
- **新设定 / 新事实**：新增了针对 `gogogo` 脚本功能的 Shell 测试，以及针对新控制器和模型的 Go 测试。这扩展了测试的覆盖范围。
- **冲突点**：未发现明确冲突。新增测试符合提升代码质量的目标。
- **建议纯化位置**：无。
- **建议修改动作**：无需修改。新增测试是良好的实践。可考虑未来在规范中补充关于 Shell 脚本测试和 Go 控制器/模型测试的目录结构或命名约定示例。
- **关联改动来源**：新增的 `tests/gogogo_*_test.sh` 和 `controller/*_test.go`、`model/referral_test.go` 等文件。

### FILE: /Volumes/RuiRui4TB/CloudBackup/Mac/code/github/al90slj23/AiLinkDog/.ai/L3#Standards/standards/10.ai-01.memory-03.skills.md
- **触发原因**：`L0#Execution` 层新增了 `skills/` 目录，用于存放技能源文件。
- **旧规范 / 旧认知**：AI 记忆体系中的技能规则（如果存在）应定义技能的来源、格式和管理方式。
- **新设定 / 新事实**：项目结构中明确了 `skills/` 目录作为技能源文件的存放位置，并计划按 `global/` 与 `project/` 分层。
- **冲突点**：未发现明确冲突。`L0` 的变更是对技能存储位置的具体化。
- **建议纯化位置**：无（假设对应的技能规则文件尚未创建或未详细规定存储路径）。
- **建议修改动作**：**仅建议补充同步**。如果 `10.ai-01.memory-03.skills.md` 文件存在，应更新其内容，指明项目级技能源文件位于 `.ai/L0#Execution/skills/` 目录下，并与 `global/` 和 `project/` 子目录的结构保持一致。如果该文件不存在，则本次无需操作。
- **关联改动来源**：`git diff` 显示对 `.ai/L0#Execution/README.md` 的修改，以及新增的 `.ai/L0#Execution/skills/` 目录。

## 纯化执行计划
1.  **立即完成（无需修改）**：
    *   确认 `.ai/L0#Execution/README.md`、`.ai/L2#Index/tech.md`、`.ai/L3#Standards/standards/09.tool-01.gogogo.md` 已为最新状态，无需操作。
    *   确认 `.ai/L3#Standards/standards/02.backend-01.json.md`、`02.backend-03.database.md`、`04.quality-02.testing.md` 无需因本次改动而更新。
2.  **待确认后执行**：
    *   **事项**：确认 `common/constants.go` 中 `SystemName` 从 “New API” 改为 “AiLink.Dog” 是否违反项目关于保护上游标识的规则。
    *   **动作**：根据确认结果，决定是否更新相关规范文件（如规则5的阐释或常量规范中的说明）。
3.  **建议性补充**：
    *   **事项**：检查 `.ai/L3#Standards/standards/10.ai-01.memory-03.skills.md` 文件是否存在。
    *   **动作**：若存在，则更新该文件，明确技能源文件位于 `.ai/L0#Execution/skills/`。若不存在，则记录此建议，待未来创建该规范文件时采纳。

## 需要用户确认的事项
1.  **`SystemName` 常量变更的合规性确认**：
    *   **变更内容**：`common/constants.go` 中的 `SystemName` 变量值从 “New API” 更改为 “AiLink.Dog”。
    *   **关联规则**：此变更可能触及“Protected Project Information — DO NOT Modify or Delete”规则（原 `AGENTS.md` 规则5）。
    *   **问题**：您是否确认此项变更符合项目政策？即，将面向用户的系统显示名称更改为本地维护品牌，而不被视为对受保护的上游项目/组织标识（nеw-аρi / QuantumNous）的修改或删除？
    *   **决策影响**：
        *   **若确认为合规**：纯化计划中关于此点的“暂停执行”状态解除，无需对规范文件进行额外操作。
        *   **若确认为不合规**：则需要回滚 `common/constants.go` 中的 `SystemName` 更改，并可能在规范中加强关于此类常量保护的说明。
