# 2026-04-19 AI 记忆体系初始化

## 背景

当前项目需要建立属于自己的 `./ai` 六层记忆体系，用于长期支撑 AI 与人类协作，而不是继续依赖旧项目的 `.ai` 内容。

## 主要来源

- 当前仓库代码结构
- 当前 `README*`
- 当前 `AGENTS.md`
- 当前 `CLAUDE.md`
- 旧项目 `ZERO` 与 `.ai` 的层次结构

## 关键取舍

- 保留旧项目六层结构与命名风格
- 不继承旧项目中与当前仓库冲突的前端规则
- 当前仓库真实使用 Semi Design，因此不接受旧项目中禁用 Semi-UI 的规则
- 新增两条正式规则：中文沟通、中文 spec

## 2026-04-19 起始声明

从 2026-04-19 开始，当前仓库明确记录 `AiLinkDog` 作为一个基于 `new-api` 的长期独立维护计划名称。

这条声明有三个核心含义：

1. 它是当前仓库未来深度优化、持续重构与长期建设的起点。
2. 未来随着本地维护深入，仓库可能无法继续采用“一键同步上游”的方式升级。
3. 即便如此，仍必须持续跟踪上游 `new-api` 的版本、修复与新功能，并评估是否合并、同步或移植同类改动。

同时必须严格遵守一条边界：

- `new-api` 与 `QuantumNous` 的受保护标识、来源关系与版权信息不得被删除或替换。

本次记录的上游基线为：

- 日期：`2026-04-19`
- 最新标签：`v0.12.14`
- 当前仓库修订：`v0.12.14-5-gf995a868`

## 初始化结果

- 建立了 `./ai` 六层骨架
- 建立了 L1/L2/L3 的首版核心文档
- 建立了初始化日志与系统关系图种子文档

## 后续工作

- 随着项目变化持续补充 L3 细节规范
- 在 L4 中持续记录重要规范调整
- 在 L5 中逐步补充更完整的系统关系视图
- 建立长期的上游版本跟踪与同步决策记录
