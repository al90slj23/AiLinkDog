任务已完成：
1. 提取旧版 V1_TechMinimal 的终端样式和动态脉冲动画，恢复并美化了 `LandingHeroCodeBlock.jsx` 的样式表现。
2. 调优了全站的 `home.css` 的全局变量，改善了不同主题下的对比度和柔和度。
3. 添加了诸如悬停动画等通用交互样式 (`.ald-lift`)，增强了网页各模块的互动感。
4. 按 `standards-sync-on-complete` 要求自动生成了 L4 工作记录。
5. 所有代码修改经过前端构建验证并已推送到远程。